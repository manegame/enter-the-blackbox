// Pure SPA: no server-side rendering, no prerendered pages — everything
// resolves client-side against adapter-static's index.html fallback.
export const ssr = false;
export const prerender = false;
